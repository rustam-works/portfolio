# Portfolio Project — Rules for Claude

## Design defaults

- **No opacity/grey by default.** Never use `opacity`, `color: grey`, or similar visual suppression unless explicitly asked. Use full-strength color from the design system (`--ids__text-RGB`) at all times.
